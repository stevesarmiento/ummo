{
  "name": "@ummo/chain",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "build": "anchor build",
    "deploy": "anchor deploy",
    "idl": "anchor idl parse -f programs/ummo_market/src/lib.rs",
    "lint:fix": "prettier . --write",
    "lint": "prettier . --check"
  },
  "devDependencies": {
    "prettier": "^2.6.2"
  }
}
